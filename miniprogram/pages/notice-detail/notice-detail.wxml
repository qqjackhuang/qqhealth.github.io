<view class="page">
  <view class="card">
    <view class="tag">{{notice.tag}}</view>
    <view class="title">{{notice.title}}</view>
    <view class="muted">{{notice.time}}</view>
    <view class="body">{{notice.content}}</view>
  </view>
</view>
